import { createStore } from 'vuex'

const store = createStore({
  state: {
    // seu estado inicial
  },
  mutations: {
    // suas mutações
  },
  actions: {
    // suas ações
  },
  getters: {
    // seus getters
  }
})

export default store